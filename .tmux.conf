# path# {{{
PATH=$PATH:$HOME/bin
PATH=$PATH:$HOME/iThief
PATH=$PATH:$HOME/.tmux/tmux-powerline
# }}}

# keymap # {{{

# prefixキーをC-wに変更する
set -g prefix C-j

# C-bのキーバインドを解除する
unbind C-b

# C-j*2でtmux内のプログラムにC-jを送る
bind C-j send-prefix

# 設定ファイルをリロードする
bind r source-file ~/.tmux.conf \; display "Reloaded!"

# window # {{{

# | でペインを縦に分割する
bind | split-window -h

# - でペインを横に分割する
bind - split-window -v

# Vimのキーバインドでペインを移動する
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind -r C-h select-window -t :-
bind -r C-l select-window -t :+

# Vimのキーバインドでペインをリサイズする
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# }}}

# }}}

# basic# {{{

# キーストロークのディレイを減らす
set -sg escape-time 1

# ウィンドウのインデックスを1から始める
set -g base-index 1

# ペインのインデックスを1から始める
setw -g pane-base-index 1

# マウス操作を有効にする
setw -g mouse

# 256色端末を使用する
set -g default-terminal "screen-256color"

# viのキーバインドを使用する
setw -g mode-keys vi

# クリップボード共有を有効にする
set-option -g default-command "reattach-to-user-namespace -l zsh"

# コピーモードの操作をvi風に設定する
bind-key -t vi-copy v begin-selection
bind-key -t vi-copy y copy-pipe "reattach-to-user-namespace pbcopy"
unbind -t vi-copy Enter
bind-key -t vi-copy Enter copy-pipe "reattach-to-user-namespace pbcopy"

# }}}

# status bar # {{{

# ステータスバーをトップに配置する
set-option -g status-position top

## ウィンドウリストの位置を中心寄せにする
set -g status-justify centre

# ステータスバーの色を設定する
set -g status-fg white
set -g status-bg black

# ペインボーダーの色を設定する
set -g pane-border-fg green
set -g pane-border-bg black

# ステータスバーを設定する
## 左パネルを設定する
set -g status-left-length 40
set -g status-right-length 40
## リフレッシュの間隔を設定する(デフォルト 15秒)
set -g status-interval 1
set-option -g status-left-length 100
set-option -g status-right-length 100

set-option -g status-right '#[bg=colour237] #(volume) #[default] #[bg=colour237] #(wifi -i)[ #(wifi -s) ] #[default] #[bg=colour237] #(battery -t -m -r)#[bg=colour237] #[default] #[bg=colour237] [%Y/%m/%d(%a) %H:%M:%S] #[default]'
# set-option -g status-left '#[bg=colour237] #(nowplaying) #[default] #[bg=colour237] #(clipboard) #[default] ' 
set-option -g status-left '' 

set -g status-utf8 on

set -g status-bg colour234
set-option -g status-left-length 100
set-option -g status-right-length 120
set-option -g default-terminal screen-256color

# }}}

# window# {{{

set -g window-style 'fg=colour247,bg=colour236'
set -g window-active-style 'fg=colour250,bg=black'
set-window-option -g window-status-bg black
set-window-option -g window-status-fg white
set-window-option -g window-status-current-attr bright

# }}}

