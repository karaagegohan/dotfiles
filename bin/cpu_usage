#!/bin/bash
cpu=$(\
  ps aux \
  | awk {'sum+=$3;print sum'} \
  | tail -n 1 \
  )

echo $cpu
# if [[ $cpu -gt 0.0 ]]; then
#   DISPLAY="#[fg=white]${cpu}%"
# elif (( $cpu < 33.0 )); then
#   DISPLAY="#[fg=blue]${cpu}%"
# elif (( $cpu < 67.0  )); then
#   DISPLAY="#[fg=green]${cpu}%"
# elif (( $cpu < 100.0  )); then
#   DISPLAY="#[fg=yellow]${cpu}%"
# else
#   DISPLAY="#[fg=red]${vol}%"
# fi
# echo "${DISPLAY}"
